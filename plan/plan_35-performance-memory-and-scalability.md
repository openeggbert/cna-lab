# 35. Performance, memory, and scalability

[Back to master plan](../plan.md)

Establish budgets and measure representative district workloads against the locked performance target (docs/performance-targets.md: baked lighting + one dynamic sun + limited shadows, ~2-4GB RAM, 512MB-1GB VRAM, 720p/30fps minimum, 1080p/60fps recommended, EasyGL as the shipped backend).

- [x] **IG-35-001 P0** — Define target hardware and frame-rate/resolution goals from docs/performance-targets.md. *(Locked minimum: 720p/30 FPS, 2 GiB RAM, 512 MiB VRAM; recommended: 1080p/60 FPS. The automated first-district capture currently runs at 1280x720.)*
- [x] **IG-35-002 P0** — Define initial CPU, GPU, memory, district-load, physics, AI, and audio budgets sized for one district (10-20 pedestrians, 5-10 physics vehicles, one sun, limited shadows). *(Initial p95 budgets are explicit in `docs/performance-targets.md` and emitted in every JSON report. End-to-end frame interval remains the GPU/user-visible gate; supported EasyGL drivers additionally provide real asynchronous `gpu_render` command-range timing as a diagnostic.)*
- [x] **IG-35-003 P0** — Instrument frame time by major subsystem (render, physics, AI, audio, district load). *(`PerformanceProfiler` captures end-to-end frame interval plus update/render/Present/physics/AI/audio/district-load/startup CPU and a non-blocking real GPU Draw-range query where supported. Render CPU is command submission; GPU timing excludes Present; Present CPU measures CNA EndDraw/swap/backend flush; frame interval remains authoritative.)*
  *Schema-completeness follow-up: `startup_cpu` is now mandatory alongside every other C++ timing
  row; top-level identity/timing metadata must be printable/positive/boolean as appropriate without
  closing the generic writer's diagnostic value set. Six malformed shapes exit 2.*
- [x] **IG-35-004 P0** — Record a baseline from the first running procedural prototype. *(Release EasyGL baseline recorded in `docs/performance-baseline.md`: intro passes 30 FPS, mixed workload fails; RAM/load pass; VRAM incomplete.)*
- [x] **IG-35-005 P1** — Track draw calls, state changes, vertices, triangles, instances, and visible objects. *(Done in JSON schema 2 as per-frame p95/average/maximum for exact Iron Gang 3D front-end submissions. Scope metadata excludes HUD backend batching and driver state deduplication; with no culling yet, every submitted scene object counts visible. The isolated 540-frame mixed run reports p95/max 18 draw calls, 56 explicit state calls, 1,768 vertices, 948 triangles, 16 geometry instances, and 67 visible objects.)*
- [x] **IG-35-006 P1** — Track texture, buffer, render-target, and transient GPU memory against the 512MB-1GB VRAM target. *(Game-owned resources are reported by logical category; the Linux DRM wrapper measures complete per-process resident buffer storage across all regions and is semantically reconstructed by the binder. Two independent full mixed AMD runs each peak at 55.57 MiB and pass the 512 MiB budget despite different GTT/VRAM placement. Physical presentation remains separate M12 scope.)*
  *Coverage-scope follow-up: raw and externally enriched captures each require their exact producer
  scope text. Two repeated hand-edited captures cannot relabel logical accounting as physical
  residency, or complete evidence as a broader guarantee, merely by agreeing with each other.*
  *Process-identity follow-up: the complete executable path/name must be one printable line before
  its `iron_gang` basename is accepted. Capture-session and external-manifest control-character
  prefixes are rejected independently.*
  *Completeness-state follow-up: `logical_tracked_bytes` and `complete_evidence` are binder-only
  fields and are rejected when `tracking_complete=false`. Two stale-field cases exit 2.*
- [x] **IG-35-007 P1** — Track district load/unload I/O, decompression, parse, upload, and activation latency (a full level load between districts, not continuous streaming). *(JSON schema 4 records each real full-level transition as world/physics unload+activation CPU, renderer rebuild/upload-submission CPU, and their total. Districts are currently procedural in-memory `PrototypeWorld`s, so I/O/decompression/parse are explicitly `null`/not applicable instead of fabricated zero-duration measurements; this must be extended if serialized district packages are introduced.)*
- [x] **IG-35-008 P1** — Track active physics bodies, contacts, queries, and physics step time for the modest vehicle/pedestrian count. *(JSON schema 5 retains the existing physics CPU timer and adds exact per-update Jolt-seam state/workload: total and active rigid bodies, active rigid-body contact manifolds, actual CharacterVirtual contacts, fixed steps, public gameplay raycasts, character collision updates, and vehicle wheel raycasts. Query categories remain separate so unlike operations are never combined into a misleading total.)*
- [x] **IG-35-009 P1** — Track simplified pedestrian/traffic/police AI counts and update time. *(Schema 6 retains peak counts and AI CPU p95/average/maximum, and adds per-update current state plus exact traffic, pedestrian, witness, and patrol loop work.)*
- [ ] **IG-35-010 P1** — Track pedestrian waypoint-graph path requests, queue time, and failures.
- [ ] **IG-35-011 P1** — Track active audio voices, decoders, streams, and bus cost. *(Partial: schema 7 reports all game-observable audio state exactly—three loaded `SoundEffect` assets, the retained engine-loop instance and whether it is playing, zero game-owned streaming assets, plus one-shot and loop control commands. CNA exposes neither fire-and-forget voice lifetime nor decoder/mixer callback, backend-channel, or bus costs; those remain explicitly unavailable rather than being reported as zero.)*
- [ ] **IG-35-012 P1** — Create representative walking, driving, interior, traffic, district-transition, and mission captures. *(Partial: deterministic `intro`, `idle`, `walk`, `drive`, `mixed`, and `mission` scenarios cover every workload that exists in the prototype. `mixed` includes traffic plus a real district transition; `mission` advances the real opening dialogue/cutscene, walks through character physics, enters through the real animation/interaction path, drives into the real warehouse trigger, and refuses to emit a report unless the mission completes. A representative interior capture remains impossible until the game has an interior gameplay space.)*
  *Full-window follow-up: a fresh isolated 900-draw Release EasyGL/Xvfb mixed run supplies 899 frame,
  900 render, and 903 update/physics/AI/audio samples plus one real transition. It validates the
  representative route but remains diagnostic rather than physical target evidence.*
  *Repeatability follow-up: a second independent full-window Xvfb run removes the pair-count blocker
  but varies from 17.127 to 24.656 ms frame p95 and triggers an honest diagnostic regression. This
  proves automation/repeatability plumbing, not physical performance.*
- [x] **IG-35-013 P1** — Create memory high-water and leak checks across repeated district load/unload cycles. *(`iron_gang_memory_soak_tests` samples Linux current/high-water RSS after warm-up while checking exact Jolt body-count restoration across every warehouse/countryside round trip. It also exercises mission reset/replay and real save/read/resume on every cycle.)*
- [x] **IG-35-014 P1** — Create content budgets enforced by validators (triangle/texture/material budgets per building/vehicle/character). *(`assets/content-budgets.json` versions explicit bootstrap limits for the warehouse, grouped four-part sedan, and test character, plus the default district-prototype source. The standard-library validator counts exact MC3/glTF triangles, material definitions, and texture resources; `build-assets.sh` enforces source coverage and limits before MC3 import.)*
- [x] **IG-35-015 P1** — Create frame-pacing and hitch histograms, including at district-transition load boundaries. *(Schema 8 bins every frame interval into mutually exclusive <=16.667, <=33.333, <=50, <=100, and >100 ms ranges; reports >33.333 ms misses, >50 ms hitches, and >100 ms severe hitches; and associates each synchronous district transition with the first following measured frame interval. Downstream validation re-derives all summary counts/percentages, correlates boundary/load samples, and requires frame p95 to occupy the histogram's nearest-rank bucket instead of trusting editable duplicates.)*
  *Maximum-correlation follow-up: `frame_interval.maximum_ms` must occupy the highest non-empty
  histogram bucket with only the producer's half-unit three-decimal rounding tolerance. A
  cross-bucket false maximum exits 2.*
  *Boundary-subset follow-up: district-boundary hitch count and maximum cannot exceed their global
  frame counterparts. Exact serialized 50.000 ms preserves either hidden-precision hitch state;
  two contradictions and one boundary case cover the rule.*
  *Boundary-occupancy follow-up: a boundary maximum must match at least one populated global
  histogram bucket. Threshold rounding may match either adjacent bucket; an interior value in an
  empty bucket exits 2.*
- [x] **IG-35-016 P1** — Create release performance reports against the locked minimum/recommended hardware targets. *(`scripts/performance_report.py` consumes one or more schema-8 captures and emits a deterministic Markdown summary with per-capture 30/60 FPS, CPU, RAM, complete-VRAM, district-load, hitch, exact requested/applied presentation, and file/session provenance. Qualification requires at least two mixed captures with distinct canonical contents and non-overlapping session intervals on explicitly declared single-line physical hardware plus one verified, cross-bundle inode-independent original/manifest/raw-artifact archive per enriched capture. Mixed runs must share exact resolution/timing/presentation/GPU-timer/workload/VRAM-tool policy and stored budgets must equal the locked evaluator constants. Copied/renamed/reformatted evidence, overlapping sessions, inconsistent policy, ambiguous/control-character identities, missing/mutated/reused sources, duplicate keys/non-standard numeric JSON, and output aliases that could overwrite evidence are rejected. Dynamic text/file names are escaped so they cannot alter Markdown/HTML structure.)*
  *Presentation-boundary follow-up: fixed proof text cannot claim physical vblank/compositor
  behavior, and unavailable reason must be empty exactly on successful apply. Three contradictory
  shapes exit 2 while real declined-Xvfb diagnostics remain readable.*
- [ ] **IG-35-017 P1** — Optimize only measured bottlenecks and record before/after evidence.
- [ ] **IG-35-018 P1** — Keep low-detail AI and rendering paths visually coherent at LOD/culling distance.
- [ ] **IG-35-019 P2** — Evaluate multithreaded culling after single-threaded costs are measured.
- [ ] **IG-35-020 P2** — Evaluate job granularity and contention only if a measured bottleneck justifies it.
- [ ] **IG-35-021 P2** — Evaluate texture streaming only after actual residency pressure appears against the VRAM budget.
- [ ] **IG-35-022 P2** — Evaluate occlusion culling after district/frustum/LOD wins are measured.
- [ ] **IG-35-023 P2** — Evaluate data-oriented component storage for proven hot loops only.
- [x] **IG-35-024 P2** — Create automated capture comparison over time. *(`scripts/performance_compare.py` compares compatible schema-8 baseline/candidate captures, reports 15 frame/CPU/GPU/load/pacing/memory metrics plus workload context and exact input hashes, and returns a CI-failing exit code only when an increase exceeds both its explicit absolute and relative tolerance. It refuses blank/multiline or cross-hardware identities, diagnostic-vs-qualifying, capture-policy, presentation, availability, memory-coverage, overlapping-session, and reverse-chronology mismatches rather than presenting them as regressions. Qualifying comparisons require and reconstruct both original/manifest/raw-artifact source bundles with candidate session after baseline; diagnostic bundles remain optional. Every supplied input is stability-checked/protected from output aliases, archive hashes are recorded with structure-safe dynamic rendering, and output is atomic.)*
  *Sample-window follow-up: qualifying mixed comparisons share the release report's 899-sample
  frame/CPU/workload floor. A fully rebound four-interval candidate exits 2, while shorter
  diagnostic comparisons remain available.*
- [x] **IG-35-025 P1** — Define scope and implement the smallest working frame profiler (CPU/GPU time per subsystem, one sun + limited shadows, EasyGL only). *(Done: CPU subsystem/Present timings plus a true asynchronous GPU Draw-range query; unsupported drivers report why and never receive a fabricated CPU fallback.)*
- [x] **IG-35-026 P1** — Add unit tests and one real-flow integration test for the frame profiler. *(Done: exact statistic/report-policy tests plus isolated full-mixed and 120-frame GPU-timed EasyGL/Xvfb flows.)*
- [x] **IG-35-027 P1** — Add logging/overlay output and documentation for the frame profiler. *(Done through versioned JSON report output and `docs/performance-targets.md`/`performance-baseline.md`; a live overlay is unnecessary for the smallest automated profiler scope.)*
  *GPU metadata follow-up: consumers require the non-blocking flag, exact Draw-only scope,
  non-negative discarded count, and support/reason consistency. Four contradictory schema cases
  exit 2 while both retained real Xvfb captures remain readable.*
- [x] **IG-35-028 P1** — Define scope and implement the smallest working memory tracker (RAM/VRAM high-water, per-category breakdown against the 2-4GB/512MB-1GB target). *(Process RAM high-water, three logical VRAM categories, and exact external `complete_process_gpu_residency_peak` sampling/binding are implemented. The two full AMD offscreen mixed runs pass at 177.1 MiB RAM and 55.57 MiB complete DRM residency; the tracker is complete even though M12 still needs physical presentation.)*
- [x] **IG-35-029 P1** — Add unit tests and one real-flow integration test for the memory tracker. *(Nine external-evidence/DRM CLI cases cover schema, identity, time/hash/hardlink, client deduplication, standard/amdgpu keys, units, and semantic artifact reconstruction. Xvfb/SOFTWARE produces no false evidence. Two real no-window EasyGL/amdgpu 900-draw mixed runs produce independent, fully verified 55.57 MiB bundles and pass every direct budget.)*
  *Canonical-token follow-up: raw UTC, source/scope, artifact-name, and SHA-256 strings cannot rely
  on trimming. Six padded capture/embedded/manifest shapes exit 2 while human labels retain their
  documented normalization.*
- [ ] **IG-35-030 P1** — Add logging/overlay output and documentation for the memory tracker. *(Partial: versioned JSON, the external evidence manifest/binder, qualification-time mandatory archive verification, distinct/non-empty source-role rules, and performance documentation cover category/evidence semantics and limitations; no live overlay or real complete backend output exists.)*
  *Evidence-consistency follow-up: the shared loader re-derives RAM known/budget state, tracked-VRAM
  budget state, and raw/enriched logical category totals. The binder validates both its raw input
  and enriched result; synthetic contradictions exit 2, while both retained real Xvfb captures pass.*
- [x] **IG-35-031 P1** — Define scope and implement the smallest working district-load profiler (load time, asset counts, memory delta per transition). *(Every exit/save-load/reset district change records source/target/reason, phase and total CPU time, procedural world-object/static-body counts, current-RSS before/after/delta where available, and logical renderer video-memory before/after/delta. Startup is correctly separate from district-transition timing.)*
- [x] **IG-35-032 P1** — Add unit tests and one real-flow integration test for the district-load profiler. *(Exact phase aggregation, asset counts, signed positive/negative deltas, and not-applicable metadata are unit-tested. An isolated 540-frame Release EasyGL/Xvfb `mixed` flow captured exactly one real WarehouseBlock -> Countryside transition.)*
- [x] **IG-35-033 P1** — Add logging/overlay output and documentation for the district-load profiler. *(Done through schema-4 JSON and `docs/performance-targets.md`/`performance-baseline.md`; a live overlay adds no value for this infrequent, loading-screen-only event.)*
  *Evidence-consistency follow-up: each detailed transition now reproduces all three phase/total
  measurement rows within explicit serialization tolerance, and its exact resident/logical-VRAM
  deltas plus procedural scope/asset counts are re-derived. Six contradictions exit 2.*
- [x] **IG-35-034 P1** — Define scope and implement the smallest working physics profiler (body/contact/query counts, step time). *(Profiling is opt-in with `--profile`; ordinary play avoids counter locks and collection. `PhysicsProfileSnapshot` consumes operation counters once per game update while retaining current body/contact state.)*
- [x] **IG-35-035 P1** — Add unit tests and one real-flow integration test for the physics profiler. *(`TestProfileSnapshotCountsStateAndConsumesOperations` proves exact bodies/steps/public rays/character updates/wheel rays plus live contacts and consume-vs-retain semantics. The isolated 540-frame Release EasyGL/Xvfb mixed flow sampled all 542 updates and reached the expected 9 bodies, 1 active body, 2 character contacts, 1 fixed step, 1 character update, and 4 wheel rays per sampled update.)*
- [x] **IG-35-036 P2** — Add logging/overlay output and documentation for the physics profiler. *(Done through schema-5 JSON plus `docs/performance-targets.md`, `performance-baseline.md`, and `validation.md`; a live overlay is unnecessary for this automated smallest scope.)*
- [x] **IG-35-037 P1** — Define scope and implement the smallest working AI profiler (pedestrian/traffic/police counts and update time). *(Schema 6 scopes `ai_cpu` to ambient traffic/pedestrian/police work, excluding mission progression, and records current traffic/pedestrian/fleeing/patrol counts plus exact traffic updates/obstacle checks, pedestrian updates/threat checks, police witness checks, and patrol updates. Transition frames retain current state and record zero suspended operations.)*
- [x] **IG-35-038 P1** — Add unit tests and one real-flow integration test for the AI profiler. *(Unit coverage proves nearest-rank statistics and exact police loop counts, including two patrol updates on the escalation tick. An isolated 540-frame Release EasyGL/Xvfb mixed flow sampled 543 updates with p95/max 2 traffic vehicles, 2 pedestrians, 4 traffic obstacle checks, 2 pedestrian threat checks, and 4 police witness checks.)*
- [x] **IG-35-039 P2** — Add logging/overlay output and documentation for the AI profiler. *(Done through schema-6 JSON plus `docs/performance-targets.md`, `performance-baseline.md`, and `validation.md`; a live overlay is unnecessary for this automated smallest scope.)*
- [x] **IG-35-040 P2** — Define scope and implement the smallest working audio profiler (active voice/stream/bus cost). *(The smallest honest scope records per-update loaded assets, retained/playing loop instances, streamed game assets, one-shot requests/successes, loop play/stop commands, and loop parameter updates alongside the existing game-owned `audio_cpu` timer. Report metadata defines the retained-voice boundary and identifies unavailable CNA backend costs without fabricating measurements.)*
- [x] **IG-35-041 P2** — Add unit tests, logging output, and documentation for the audio profiler. *(Nearest-rank statistics and schema-7 scope/output are unit-tested. An isolated 540-frame Release EasyGL/Xvfb mixed flow sampled 544 updates: audio CPU was 0.010 ms average / 0.019 ms p95 / 0.095 ms maximum, all four one-shot requests succeeded, and the expected one retained engine loop reached playing state.)*
  *Cross-workload evidence follow-up: every producer-defined render/physics/AI/audio metric and fixed
  scope is required; count summaries enforce zero/one/maximum and integer p95/max invariants. Six
  corrupt schema shapes exit 2 without asserting relationships the generic writer cannot promise.*
- [x] **IG-35-042 P1** — Define scope and implement the smallest working performance report generator (per-release summary against locked hardware targets). *(The standard-library Python CLI validates schema/histogram integrity, evaluates raw measurements against locked constants rather than trusting report booleans, separates `DIAGNOSTIC`/`FAIL`/`PASS`, and refuses to qualify Xvfb/llvmpipe, offscreen/headless/surfaceless presentation, unacknowledged presentation, or incomplete/unverifiable VRAM.)*
- [x] **IG-35-043 P1** — Add unit tests and one real-flow integration test for the performance report generator. *(A fourth CTest now runs seven CLI cases covering diagnostic virtual input/provenance, a two-independent-archive same-policy/non-overlapping physical pass with bundle hashes, session/resolution/VRAM-tool repeatability mismatches, locked-budget/missing/mutated archive refusal, copied-capture and swap failures including request/applied and v-sync contradictions, stale schemas/non-standard numeric JSON/corrupt measurement/histogram-derived pacing/boundary fields, adversarial Markdown names, and atomic output preservation across direct/hardlink aliases. The real isolated schema-8 capture generates `DIAGNOSTIC` with the expected one-run/swap/VRAM blockers and exact session/hash provenance.)*
  *Schema-type follow-up: root `schema_version` must be a non-boolean JSON integer exactly equal to
  8. A floating-point `8.0` exits 2 instead of exploiting Python numeric equality.*
  *Derived-check follow-up: frame minimum/recommended, aggregate CPU, and nullable district-load
  producer booleans must agree with stored sample/p95 direction except at an irrecoverably rounded
  exact budget boundary. Four contradiction cases exit 2 and one boundary case remains readable.*
  *Qualification-boundary follow-up: a structurally valid producer failure at exact serialized
  equality still blocks frame, aggregate-CPU, or mixed-district qualification. The 30/60 FPS table
  uses the same producer decisions; three exact-boundary cases cover the rule.*
  *Numeric-range follow-up: shared JSON parsing rejects non-finite decoded floats and integers
  outside the C++ producer's 64-bit domain, including in unknown fields. Two overflow shapes exit 2
  without a traceback.*
  *Sample-window follow-up: each qualifying mixed capture needs at least 899 frame, budgeted-CPU,
  and workload-summary samples, matching the documented 900-draw representative run after its
  baseline-only first frame. A four-sample shape is `FAIL`, not release evidence.*
- [x] **IG-35-044 P2** — Document usage of the performance report generator. *(README and performance-target docs include diagnostic and qualifying commands, exact capture/bundle provenance, mandatory qualification archive bundles, atomic non-destructive output, explicit hardware-assertion semantics, exit behavior, and the rule that successful report generation does not imply a passing gate.)*
- [x] **IG-35-045 P2** — Define scope and implement the smallest working content budget validator (triangle/texture/material limits at authoring/import time). *(Bootstrap triangle ceilings use 4x current committed geometry; material/texture reserves are explicit per asset group and are not claimed as final production limits. Texture count is enforced now; texture resolution remains under aggregate M12 VRAM evidence until representative production textures exist.)*
- [x] **IG-35-046 P2** — Add unit tests and CI wiring for the content budget validator. *(`iron_gang_content_budget_tests` makes CTest 5/5 and covers the real committed sources, an actionable over-budget failure, unknown MC3 primitive rejection, malformed glTF triangle counts, and unbudgeted import-source rejection.)*
- [x] **IG-35-047 P2** — Document usage and failure modes of the content budget validator. *(README/performance-target/validation continuity docs record policy rationale, current exact counts, CLI/import behavior, exit codes, unsupported-primitive behavior, and the deliberate texture-resolution limitation.)*
- [x] **IG-35-048 P2** — Define scope and implement the smallest working frame-pacing/hitch detector (combines pacing histogram and hitch flagging into one tool). *(Implemented directly in the existing bounded JSON capture: no second recorder or raw-sample duplication. Threshold comparisons and histogram inclusivity are explicit, with count/percentage output and district-boundary coverage.)*
- [x] **IG-35-049 P2** — Add unit tests and one real-flow integration test for the frame-pacing/hitch detector. *(Unit coverage proves all five buckets, strict 50/100 ms thresholds, and first-frame-after-transition selection. An isolated 540-frame Release EasyGL/Xvfb mixed flow sampled 539 intervals and found one 55.936 ms non-transition hitch, zero severe hitches, and a 17.345 ms transition boundary.)*
- [x] **IG-35-050 P2** — Document usage of the frame-pacing/hitch detector. *(Schema-8 semantics, thresholds, real-flow baseline, Xvfb limitation, and JSON reproduction are documented in the performance target/baseline/validation continuity docs; a separate live overlay is unnecessary for bounded automated capture.)*
  *Scope-integrity follow-up: the report loader requires the exact producer meanings for ordinary
  frame intervals and post-`RecordDistrictLoad` boundaries. Two relabelled-scope cases exit 2, so
  otherwise consistent histogram data cannot claim a different sampling boundary.*
- [x] **IG-35-051 P2** — Define scope and implement the smallest working memory-leak soak test (repeated district load/unload, mission replay, save/load cycles). *(The no-window executable has a bounded default/CTest run of 200 cycles and configurable `--cycles` up to 100,000. After 20-cycle warm-up it checks <=8 MiB current-RSS growth, <=16 MiB peak growth, <=32 KiB/cycle linear trend, and exact restoration of 8 WarehouseBlock static bodies.)*
- [x] **IG-35-052 P2** — Add CI wiring and documentation for the memory-leak soak test. *(A sixth CTest runs 200 cycles with a 60-second timeout and `soak;performance` labels. Real 200- and 5,000-cycle runs both finished with only +4 KiB current/peak RSS; the long run's slope rounded to 0 B/cycle. Docs distinguish this lifecycle leak guard from graphical/physical qualification.)*
