# 24. Mission framework and scripting

[Back to master plan](../plan.md)

Represent missions as versioned, testable state graphs, data-driven enough to carry a
15-20 mission campaign across multiple districts without becoming unmanageable. Logic
that cannot be expressed as declarative data uses a small, engine-evaluated
condition/action expression syntax, not a general-purpose sandboxed scripting language
(no embedded Lua VM, no script API surface to secure).

## Core mission data and flow

- [x] **IG-24-001 P0** — Replace prototype hard-coded mission state with versioned data while preserving current behavior. *(`MissionDefinition`/`LoadMissionDefinition` (`include/`/`src/Missions/MissionDefinition.hpp/.cpp`) load a versioned JSON file (`assets/missions/prologue.mission.json`) defining each state's id/objective text/transition condition/next-state; `PrototypeMission` now looks these up instead of a hardcoded switch, but keeps `PrototypeMissionState` as a fixed enum for `SaveGame`'s existing int-based format and public-API compatibility. Behavior verified identical to the original hardcoded flow by `TestMissionFlow` (hardcoded default, unchanged) and the new `TestMissionLoadsCommittedFile` (same flow, loaded from the real file) both passing with the same assertions.)*
- [ ] **IG-24-002 P0** — Define mission, state, objective, condition, action, trigger, checkpoint, failure, and completion IDs. *(Partial: state ids are free-form and authoritative (IG-24-018 removed the fixed five-value enum); the seven engine fact ids are declared by `CreatePrototypeMissionFacts()`; mission-variable ids are declared per file; action ids are `set`/`log`; and **completion/failure ids now exist** as a state's `"outcome"` (`completed`/`failed`), which is what `IsCompleted()`/`IsFailed()`/`IsFinished()` read -- no engine code keys off a particular state id any more. Still missing: trigger and checkpoint ID concepts -- this prototype has no triggers beyond the warehouse zone. Checkpoint ids are a state's own id -- a state declares `"checkpoint": true` rather than carrying a separate identifier (IG-24-010).)*
- [x] **IG-24-003 P0** — Create a mission validation tool (script or command, not a GUI editor) that finds missing references and unreachable states. *(Smallest form: inline validation inside `LoadMissionDefinition` itself, not a separate script/command -- rejects duplicate state ids, an `initialState`/`next` that doesn't match any state id, an unrecognized condition name, and an empty state list, each with an actionable error message. `TestMissionValidationRejectsMalformedData` covers all five cases plus a missing-file case. No separate "unreachable state" detection (linear graph today has no way to become unreachable) and no standalone CLI tool a content author could run independently of the game itself.)*
- [x] **IG-24-004 P0** — Create one complete data-driven vertical-slice mission covering the existing prologue delivery flow. *(`assets/missions/prologue.mission.json`: the exact 5-state prologue delivery flow -- listen to dialogue, reach the sedan, enter it, drive to the warehouse, complete -- as data. `PrototypeMission::LoadMission` loads it in `IronGangGame::Initialize`, falling back to an identical hardcoded default on any load/validation failure.)*
- [ ] **IG-24-005 P1** — Create typed mission variables (bool/int/float/string) scoped per mission and per campaign. *(Partial: per-mission scope is done. `MissionValue`/`MissionValueType` (`include/IronGang/Missions/MissionValue.hpp`) is the four-type value; `MissionContext` (`MissionContext.hpp`) is the symbol table holding a mission's declared variables alongside the engine's read-only facts, with typed assignment (a wrong-typed `Set*` is rejected, not coerced), duplicate/shadow rejection, a 64-variable cap, and `ResetVariables()` for retry. Mission files declare them under `"variables"` (schema version 2). Verified by `TestMissionVariablesEnforceTypes` plus the committed `prologue.mission.json`, which now declares four. **Per-campaign scope is not implemented** -- there is no campaign object to own it yet (IG-24-021/046).)*
- [ ] **IG-24-006 P1** — Create entity, area, dialogue, cinematic, timer, inventory, vehicle, and wanted-state conditions. *(Partial: ten engine facts exist and are readable from any condition expression -- dialogue (`dialogue_finished`), vehicle (`player_driving`, `player_vehicle_distance`), area (`player_in_warehouse_goal`, `vehicle_in_warehouse_goal`), and **wanted state** (`police_alerted`, `police_chasing`, `police_chase_seconds`, the last backed by a new `PoliceSystem::GetChaseSeconds()`). The police facts are pushed in by the game through `PrototypeMission::SetFact()` rather than derived from `Update()`'s arguments, because `PoliceSystem` is the game's -- that is the extension point any later subsystem uses. Because a condition is an expression, combinations need no engine support (`player_driving && vehicle_in_warehouse_goal` replaced the fused signal). Still missing: entity-reference, cinematic, timer, and inventory facts -- none of those subsystems expose per-mission state yet.)*
- [ ] **IG-24-007 P1** — Create spawn, despawn, enable, disable, move, play, set, wait, and branch actions. *(Partial: `set` and `log` are implemented as `MissionAction`, run on state entry in file order, exactly once per entry, capped at 16 per state, and validated at load. **`branch` is covered differently and deliberately**: branching is a property of a state's `"transitions"` list (IG-24-024), evaluated in file order with the first matching condition winning, rather than an imperative action -- an action that jumps would give a state two competing notions of where it goes next. Still missing: spawn/despawn/enable/disable/move/play/wait, each of which needs an entity or timer concept the prototype does not have.)*
- [ ] **IG-24-008 P1** — Create objective text and progress events.
- [x] **IG-24-009 P1** — Create failure reasons and retry policies. *(A state ends the mission in failure with `"outcome": "failed"` and explains itself with `"reason"`, which `PrototypeMission::GetFailureReason()` returns and the HUD/window title show as `Mission failed: <reason>` in place of the objective line (`MissionStatusLine` in `IronGangGame.cpp`). A reason on a non-failing state is a load error, since nothing would read it. Two retry policies, declared at the mission's top level as `"retry"`: `checkpoint` (the default) and `mission_start`; `PrototypeMission::Retry()` honours them, falling back to a restart when no checkpoint has been reached yet. Verified by `TestMissionCheckpointRetryAndFailureReason`. Note this is the framework: no gameplay path in the prologue mission can currently fail, so the failure route is exercised by tests rather than by play.)*
- [x] **IG-24-010 P1** — Create checkpoint boundaries and terminal-state application on failure/retry. *(A state declares `"checkpoint": true`; entering it records its id plus the mission's variables **as they stand once its entry actions have run**, so a retry restores that state without re-running them and without double-counting anything they incremented. A checkpoint state may not also be an outcome (a state that ends the mission cannot be retried from) -- a load error. Retrying does not consume the checkpoint, so repeated failures return to the same place; `Reset()` discards it, `Retry()` honours it. Covered by `TestMissionCheckpointRetryAndFailureReason`.)*
- [ ] **IG-24-011 P1** — Create mission-owned entity references that survive a district load/unload transition.
- [ ] **IG-24-012 P1** — Create mission pause/resume across cinematics and menus.
- [x] **IG-24-013 P1** — Create a small condition/action expression syntax (comparisons, boolean combinators, arithmetic on mission variables) for logic not expressible as declarative data. This is evaluated directly by the engine; it is explicitly not a general-purpose embedded scripting language and has no sandboxing/API-surface project attached to it. *(`MissionExpression` (`include/IronGang/Missions/MissionExpression.hpp`, `src/Missions/MissionExpression.cpp`): tokenizer, recursive-descent parser, static type check, and evaluator over a flat node array. Supports `|| && ! == != < <= > >= + - * /`, unary minus, parentheses, int/float/bool/single-quoted-string literals, and identifiers resolving to declared facts or variables. Chained comparison is rejected rather than silently reinterpreted; `&&`/`||` short-circuit; int/float mix promotes to float. There are no statements, calls, loops, assignments, or engine API reachable from an expression, so the non-goal holds by construction. Mission files use it under `"when"` (`"condition"` remains the version-1 spelling). Documented in `docs/mission-scripting.md`.)*
- [x] **IG-24-014 P1** — Enforce evaluation limits (max expression depth/step count, no recursion) so a malformed mission file cannot hang or corrupt engine state. *(Source length 512, tokens 128, operations 96, nesting depth 16, evaluation steps 256, variables per mission 64, entry actions per state 16 -- all named constants in `MissionExpression.hpp`/`MissionContext.hpp`/`MissionDefinition.hpp`, all enforced with an actionable message. Recursion is impossible: the grammar has no calls, and parse depth is capped before the stack can grow. Division by zero is an evaluation error rather than an infinity. State corruption is prevented by type-checking every expression at load: a mission that would misbehave does not load at all. Verified by `TestMissionExpressionRejectsMalformedInput` (20 malformed sources plus the length/depth/token limits and the divide-by-zero path).)*
- [ ] **IG-24-015 P1** — Create a mission debug overlay with forced state transitions.
- [x] **IG-24-016 P1** — Log mission state/condition/action transitions through the existing logging path for debugging; no dedicated event-replay system. *(`PrototypeMission::Update()` logs every transition as `[IronGang][mission] <mission>: <from> -> <to> (<condition source>)` on `std::cerr`, the same path the rest of the game already uses; `log` actions and any runtime condition/action failure go through the same prefix. No replay system was added, as the task requires.)*
- [ ] **IG-24-017 P1** — Create deterministic mission scenario tests.
- [x] **IG-24-018 P1** — Create version migration for mission state. *(Both halves done. **File schema:** `version` is validated against `kMinMissionFileVersion`/`kMaxMissionFileVersion` (1-2), an unsupported version is a load error naming the supported range, and version-1 files load unchanged -- `variables`/`onEnter`/`outcome` are rejected in one, every gate-M7 condition name is still a declared bool fact, and a version-1 file's terminal `completed` state is recognised as the success outcome by the compatibility rule in `LoadMissionDefinition`. **Persisted state:** the save now stores `mission_state_id=<id>`; a save from the earlier format (a 0-4 index into the fixed `PrototypeMissionState` enum) is migrated on read through the exact mapping that enum had, and an out-of-range index or a save with neither field is rejected rather than silently clamped. This is what removed the five-fixed-state-ids restriction on mission files. Verified by `TestSaveMigratesLegacyMissionState` and `TestMissionStateIdsAreNotAFixedSet`.)*
- [ ] **IG-24-019 P1** — Create safe behavior when content referenced by a save is missing. *(Partial: mission state and variables are covered. `PrototypeMission::SetStateId()` refuses a state id the loaded mission does not define and leaves the mission where it is (`IronGangGame::LoadPrototype` reports it), rather than stranding it in a state with no objective, condition, or way out; `ApplyVariables()` skips and reports a saved variable the mission no longer declares or whose type changed; `SaveGame::Read` skips a malformed `mission_var.*` line instead of failing the load. Covered by `TestMissionVariablesSurviveSaveLoad`, `TestMissionStateIdsAreNotAFixedSet`, and `TestMissionCheckpointSurvivesSaveLoad` (a checkpoint into a state the mission no longer defines is dropped rather than sending a retry nowhere). Still missing: the same treatment for a saved district or model id that no longer exists.)*
- [x] **IG-24-020 P1** — Create a mission dependency and prerequisite graph across the campaign. *(`CampaignDefinition`/`CampaignState` (`include/IronGang/Missions/CampaignDefinition.hpp`) load `assets/missions/campaign.json`: mission id, file path, title, and prerequisite ids. Validation refuses every shape that produces an unfinishable campaign -- unsupported version, empty/duplicate id, empty path, a prerequisite naming a mission that is not there, a mission requiring itself, **a dependency cycle** (reported as the path that loops, not merely "cycle detected"), and a campaign where nothing is available from the start. Covered by `TestCampaignGraphUnlocksAndRejectsCycles`, including a three-mission chain and a longer cycle.)*
- [ ] **IG-24-021 P1** — Create campaign chapter and unlock state for the full 15-20 mission, multi-district campaign. *(Partial: the mechanism works and **two** missions use it. `CampaignState` tracks completion and offers the first available mission; `IronGangGame` starts it, and on completion marks it done and starts what that unlocks -- verified in a real `--profile-scenario mixed` run where the prologue completes and "Out of Town" starts by itself. The second mission (`assets/missions/countryside_run.mission.json`) is a real one: drive out of the block, cross into the countryside (via the new `current_district` fact), pull into the farmhouse yard (a delivery trigger added to that district), with two checkpoints and a failure branch on `vehicle_disabled` -- which is what finally uses the wreck fact plan_17 IG-17-015 added. **Missing:** chapters as a concept above missions, and 15-20 missions rather than two. Campaign progress **is** saved and restored (IG-24-049).)*
- [ ] **IG-24-022 P2** — Create reusable mission shape templates (delivery, tailing, chase, escort, infiltration, conversation, escape) as data presets, not a generator tool.
- [ ] **IG-24-023 P2** — Create optional objective and rating support only if game design uses it.
- [ ] **IG-24-024 P2** — Create non-linear mission choice support after the first linear campaign slice works. *(Partial: the **graph** is non-linear now. A state declares `"transitions": [{"when":…,"next":…}, …]` (at most `kMaxMissionStateTransitions`), evaluated in file order with the first matching condition winning, so a state can branch on any fact or variable; the single-transition `"when"`/`"next"` shorthand produces one entry in the same list. The committed prologue uses it to branch between delivering the cargo and being busted. Covered by `TestMissionBranchesOnFirstMatchingTransition` plus seven new rejection cases. Still missing: **player choice** -- nothing lets the player pick a branch (dialogue options, an accept/decline prompt); every branch today is decided by world state.)*

## Mission graph runtime

- [x] **IG-24-025 P0** — Define the scope, public API, and explicit non-goals (no embedded scripting language) of the mission graph runtime. *(`MissionDefinition.hpp` states the graph/action/versioning contract; `MissionExpression.hpp` states the expression language's grammar, limits, and the explicit non-goal (no statements, calls, loops, assignments, or engine API surface inside an expression -- a mission file can only reach declared facts and variables). Originally recorded when conditions were a fixed enum; IG-24-013 replaced that with the expression evaluator without weakening the non-goal.)*
- [x] **IG-24-026 P0** — Implement the smallest deterministic reference path: load one mission, advance through states, reach completion. *(`LoadMissionDefinition` + `PrototypeMission::LoadMission`/`Update`/`IsCompleted` -- see IG-24-001/004.)*
- [x] **IG-24-027 P1** — Add input validation and actionable failure reporting for malformed mission data. *(See IG-24-003.)*
- [x] **IG-24-028 P1** — Add unit tests and one integration scenario exercising the runtime in a running game flow. *(`TestMissionLoadsCommittedFile`/`TestMissionValidationRejectsMalformedData` in `tests/CoreTests.cpp`; the "running game flow" integration is `IronGangGame::Initialize` calling `LoadMission` and a `--smoke` run confirming no crash and no fallback-triggered error message.)*
- [x] **IG-24-029 P1** — Define save/checkpoint serialization and restoration for in-progress mission state. *(State: the save stores the state **id** (`mission_state_id`), migrating an older int-indexed save on read -- see IG-24-018. Variables: `SaveSnapshot::missionVariables` is written as one `mission_var.<name>=<type>:<value>` line per variable (so a string value may contain any character except a newline) and restored through `PrototypeMission::ApplyVariables()`. Restoring deliberately does **not** re-run entry actions -- the run being resumed already did -- which is why `SetStateId()` and `Reset()` are separate. `MissionValue::ToText()`/`Parse()` round-trip every type exactly (float uses the shortest round-trip form). Verified by `TestMissionVariablesSurviveSaveLoad`. The mission's checkpoint is serialized alongside it -- see IG-24-044.)*
- [x] **IG-24-030 P2** — Add debug logging/inspection and document usage, invariants, and common failure modes. *(Logging: see IG-24-016. Inspection: `PrototypeMission::GetContext()`/`GetDefinition()`/`TryGetVariable()`. Documentation: `docs/mission-scripting.md` -- schema, variables, fact table, grammar, actions, the limit table, logging format, and a failure-mode table mapping each error message to its cause.)*

## Condition/action expression evaluator

- [x] **IG-24-031 P1** — Define the scope and public API of the condition/action expression evaluator described in IG-24-013/IG-24-014. *(`MissionExpression.hpp`'s header comment states the grammar, the non-goals, and every limit; the public API is `Compile(source, context, out, error)` / `GetResultType()` / `Evaluate()` / `EvaluateBool()` / `IsEmpty()` / `GetSource()`. `MissionContext` is the accompanying symbol table (facts vs variables). Compilation is separated from evaluation on purpose: types are resolved once at load, values every frame.)*
- [x] **IG-24-032 P1** — Implement the smallest deterministic reference path: evaluate one condition, execute one action. *(The committed `assets/missions/prologue.mission.json` is that path end to end: `"when": "player_vehicle_distance <= handover_radius"` evaluates a fact against a mission variable, and `{"action": "set", "variable": "cargo_secured", "value": "true"}` executes on entry. `TestMissionEntryActionsRunOncePerEntry` and `TestMissionExpressionEvaluatesTypedOperations` cover it deterministically; the `--profile-scenario mission` run drives it through the real game loop.)*
- [x] **IG-24-033 P1** — Add input validation, depth/step limits, and actionable failure reporting for malformed expressions. *(Every compile error names the problem and a 1-based column, e.g. `unknown identifier "foo" (not a declared mission fact or variable) at column 5`; the loader prefixes the mission state and file. Type errors are reported with both operand types. Limits as in IG-24-014. Verified by `TestMissionExpressionRejectsMalformedInput` and by the 17 malformed-file cases in `TestMissionValidationRejectsMalformedData`.)*
- [ ] **IG-24-034 P1** — Add unit tests and one integration scenario covering the condition/action set from IG-24-006/IG-24-007. *(Partial: everything currently implemented is covered -- `TestMissionExpressionEvaluatesTypedOperations`, `TestMissionExpressionRejectsMalformedInput`, `TestMissionVariablesEnforceTypes`, `TestMissionEntryActionsRunOncePerEntry`, `TestMissionVariablesSurviveSaveLoad`, and the two existing flow tests, plus the `--profile-scenario mission` integration run through the real game loop. This stays open because IG-24-006/IG-24-007 themselves are partial: there is nothing yet to test for entity/cinematic/timer/inventory/wanted conditions or spawn/despawn/enable/disable/move/play/wait actions.)*
- [x] **IG-24-035 P2** — Add debug logging/inspection and document the expression syntax for mission authors. *(`docs/mission-scripting.md` documents the grammar, typing rules, short-circuiting, string-literal quoting, the fact table, both actions, every limit, the transition log format, and a failure-mode table. Logging and inspection as in IG-24-030.)*

## Objective tracker

- [ ] **IG-24-036 P1** — Define the scope and public API of the objective tracker.
- [ ] **IG-24-037 P1** — Implement the smallest deterministic reference path: track one objective from active to complete/failed.
- [ ] **IG-24-038 P1** — Add unit tests and one integration scenario exercising objective text/progress events.
- [ ] **IG-24-039 P1** — Define save/checkpoint serialization and restoration for objective progress.
- [ ] **IG-24-040 P2** — Add debug logging/inspection and document usage.

## Checkpoint coordinator

- [x] **IG-24-041 P1** — Define the scope and public API of the checkpoint coordinator. *(Deliberately not a separate class: a checkpoint is one state id plus one variable snapshot, and every operation on it needs the mission's own definition and context, so it lives on `PrototypeMission` as `HasCheckpoint()`/`GetCheckpoint()`/`ApplyCheckpoint()`/`Retry()`/`GetRetryPolicy()` with `MissionCheckpointSnapshot` as the serializable value. A standalone coordinator would only forward to those. Documented in `docs/mission-scripting.md`'s "Failing, checkpoints, and retry" section.)*
- [x] **IG-24-042 P1** — Implement the smallest deterministic reference path: reach a checkpoint, fail, retry from it. *(`TestMissionCheckpointRetryAndFailureReason` is literally that path: a mission advances into a checkpoint state whose entry action sets a counter, moves to a failing state, retries, and must come back to the checkpoint state with the recorded counter -- not the declared one, and not the counter incremented a second time. The same mission under `"retry": "mission_start"`, and a checkpoint retry before any checkpoint exists, must both restart instead.)*
- [x] **IG-24-043 P1** — Add unit tests and one integration scenario covering failure/retry policies from IG-24-009. *(Unit tests: `TestMissionCheckpointRetryAndFailureReason` (both policies, the no-checkpoint fallback, the failure reason, no double-counted entry action), `TestMissionCheckpointSurvivesSaveLoad`, and `TestMissionBranchesOnFirstMatchingTransition`. Integration scenario: `TestPrologueFailsAndRetriesUnderPoliceChase` drives the **committed** `assets/missions/prologue.mission.json` and the **real** `PoliceSystem` frame by frame -- speed past a witness, get chased, fail on the mission's own `police_chase_seconds > 25` branch with its own reason, retry back to the `drive_to_warehouse` checkpoint with `cargo_secured` still set, then verify that a cleared chase does not immediately re-fail the mission and that the delivery still completes exactly once. In the running game the same loop is `IronGangGame::RetryMission()` on the R key, which also resets the police response (via `RespawnTrafficAndPedestrians`) and restores the player/vehicle from the world snapshot `CaptureMissionCheckpointWorld()` took when the checkpoint was recorded; a `--profile-scenario mission` run confirms the checkpoint is recorded and the mission still completes normally when no chase occurs.)*
- [x] **IG-24-044 P1** — Define save/checkpoint serialization and restoration guarantees. *(The save writes `mission_checkpoint_state_id` plus one `mission_checkpoint_var.<name>=<type>:<value>` line per recorded variable; a mission with no checkpoint writes neither, and an older save without them loads as "no checkpoint". Restoration is fail-safe: a checkpoint naming a state the loaded mission no longer defines is dropped **entirely** (a retry that went nowhere would be worse than a restart), and a variable whose declaration or type changed is dropped individually -- both reported through `ApplyCheckpoint`'s warnings, which `IronGangGame::LoadPrototype` prints. Verified by `TestMissionCheckpointSurvivesSaveLoad`.)*
- [x] **IG-24-045 P2** — Add debug logging/inspection and document usage. *(Logging: recording a checkpoint and every retry go through the same `[IronGang][mission]` path as transitions, naming the checkpoint state and which policy the retry used. Inspection: `HasCheckpoint()`/`GetCheckpoint()`/`GetRetryPolicy()`/`GetFailureReason()`. Documentation: the "Failing, checkpoints, and retry" section of `docs/mission-scripting.md`, plus the schema table and two new failure-mode rows.)*

## Campaign graph (chapters, unlocks, dependencies)

- [x] **IG-24-046 P1** — Define the scope and public API of the campaign graph covering IG-24-020/IG-24-021. *(`CampaignDefinition` is content (missions and their prerequisites); `CampaignState` is progress (which are done, therefore which are available). They are separate types on purpose: one is shipped data, the other belongs in a save, and merging them would put content in the save file. API: `Find`, `Reset`, `MarkCompleted`, `IsCompleted`, `IsAvailable`, `NextAvailable`, `IsFinished`, `GetCompleted`/`SetCompleted`. `MarkCompleted` and `SetCompleted` ignore unknown ids so restored progress cannot inject a mission the campaign no longer has.)*
- [x] **IG-24-047 P1** — Implement the smallest deterministic reference path: unlock one chapter after completing its prerequisite mission. *(The committed campaign is exactly that path: `countryside_run` requires `prototype_delivery` and is unavailable until it is done. Asserted in `TestCampaignGraphUnlocksAndRejectsCycles` against the **committed** file, and observed end to end in a `--profile-scenario mixed` run.)*
- [ ] **IG-24-048 P1** — Add unit tests and one integration scenario covering the full 15-20 mission dependency graph. *(Partial: the graph logic is covered for two committed missions plus synthetic three-mission chains and cycles (`TestCampaignGraphUnlocksAndRejectsCycles`), and the second mission's own flow, failure branch, and checkpoint retry are covered by `TestCountrysideMissionRunsAndFailsOnAWreck`. There is no 15-20 mission graph to test because there are two missions.)*
- [x] **IG-24-049 P1** — Define save/checkpoint serialization and restoration for campaign progress. *(The save carries `mission_id` (which mission is being played) and one `campaign_completed.<id>=1` line per finished mission -- additive, so an older save simply claims no progress rather than inventing some. **Restore order is the whole difficulty and is written down at the call site**: campaign progress first, then the right mission file (`StartMission` resets the mission), and only then the state id, variables, and checkpoint that belong to it. Restoring the state before loading the mission would apply it to whichever mission happened to be loaded; loading the mission afterwards would reset everything just restored. Progress naming a mission the campaign no longer contains is dropped on restore (`CampaignState::SetCompleted`), and a save naming an unloadable mission keeps the current one with a warning rather than failing the load. Covered by `TestCampaignProgressSurvivesSaveLoad`; a real `--profile-scenario mixed` run's autosave contains `mission_id=countryside_run` and `campaign_completed.prototype_delivery=1` after the campaign advanced.)*
- [ ] **IG-24-050 P2** — Add debug logging/inspection and document usage.

## Mission entity, timer, and variable binding

- [ ] **IG-24-051 P1** — Define the scope and public API for binding mission logic to entity references, timers, and typed variables (IG-24-005/IG-24-011).
- [ ] **IG-24-052 P1** — Implement the smallest deterministic reference path: a mission timer expiring updates a mission variable an entity reference depends on.
- [ ] **IG-24-053 P1** — Add unit tests and one integration scenario covering entity-reference survival across a district load (IG-24-011).
- [ ] **IG-24-054 P1** — Define save/checkpoint serialization and restoration for timers, variables, and entity bindings.
- [ ] **IG-24-055 P2** — Add debug logging/inspection and document usage.
